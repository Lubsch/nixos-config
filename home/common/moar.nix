# pager - alternative to less
# NOTE not enabled because the text is ugly
# TODO find alternative (including journald integration)
{ pkgs, ... }:
{
  # home.packages = [ pkgs.moar ];
  # home.sessionVariables.PAGER = "moar";
}
